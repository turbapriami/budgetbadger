import { RENDER_TRANSACTIONS } from '../actions/types'

const INITIAL_STATE = { transactions: [] };

export default (state = INITIAL_STATE, action) => {
  // switch (action.type) {
  //   case RENDER_TRANSACTIONS: 
  //     return { ...state, videos: action.payload };
  // }
  return state;
}